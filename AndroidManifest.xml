<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.example.adnroid.camera"
      android:versionCode="1"
      android:versionName="1.0">
    <application android:icon="@drawable/icon" android:label="@string/app_name">
        <activity android:label="@string/app_name" android:name="Main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="Hello" android:screenOrientation="landscape" /><!-- check here -->
        <activity android:name="AutoFocus" android:screenOrientation="landscape" /><!-- check here -->
        <activity android:name="Parameters" android:screenOrientation="landscape" /><!-- check here -->
        <activity android:name="Overlay" android:screenOrientation="landscape" /><!-- check here -->
        <activity android:name="CameraPreferences" />
	</application>
    <uses-permission android:name="android.permission.CAMERA" /><!-- check here -->
    <uses-feature android:name="android.hardware.camera" /><!-- check here -->
    <uses-feature android:name="android.hardware.camera.autofocus" /><!-- check here -->
    <uses-feature android:name="android.hardware.camera.flash" /><!-- check here -->
</manifest>
